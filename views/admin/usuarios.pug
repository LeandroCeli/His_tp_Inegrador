h2(class="text-2xl font-bold mb-6 text-gray-800") Gestión de Usuarios


table(class="w-full border-collapse")
    thead
      tr
        th Nombre
        th Apellido
        th Email
        th Rol
        th Activo
    tbody
      each usuario in usuarios
        tr
          td= usuario.nombre
          td= usuario.apellido
          td= usuario.email
          td= usuario.rol
          td= usuario.activo ? 'Sí' : 'No'